# AI 员工验收报告

## 1. 验收结论

截至 2026 年 8 月 6 日，AI 员工的生产底座、钉钉消息监听、草稿生成、人工审批、项目能力网关、正式记忆、运行监控、备份恢复和复用初始化均已完成技术验收。

当前保持“影子运行”：可以监听、判断、生成草稿和形成受控工作计划，但自动发送和计划自动执行没有开启。原因不是系统故障，而是真实人工标注尚未达到放量门槛。

## 2. 已验证项目

| 验收项 | 当前证据 | 结论 |
|---|---|---|
| 自动化测试 | 本地全量测试 282 项：258 通过、24 项因未注入测试库跳过、0 失败；云端检查按提交完整 SHA 在 Node 22/24 与 PostgreSQL 16 上执行全量测试、打包、隔离复用及服务回退验收 | 本地通过；每次发布以目标提交对应的云端回执为准 |
| 生产数据库 | PostgreSQL 迁移 001—016 已应用；现场只读回读为 expected 16、applied 16、pending 0、校验和漂移 0、意外迁移 0 | 通过 |
| 常驻运行 | 监听、Worker、执行器、健康、管理台、告警、消息对账、记忆来源复核、备份共 9 个应用 LaunchAgent；均指向当前不可变版本目录 | 通过 |
| 生产健康 | `production:verify` 返回健康服务与管理台均验证成功 | 通过 |
| 消息完整性 | 最近一次 24 小时 DWS 消息源对账无剩余缺失 | 通过，继续持续观察 |
| 入口可用性 | 已产生真实分钟采样；缺测按不可用，未满 30 天时不判定达标 | 观察中 |
| 记忆一致性 | 重复候选、冲突候选和已确认双活冲突均有指标；生产回读均为 0 | 通过 |
| 依赖安全 | `npm audit --omit=dev` 为 0 个已知漏洞；提交 `34d7506` 的 GitHub CodeQL 自动扫描 `31078887710` 通过 | 通过 |
| 发布包 | 只包含源码、中文文档、迁移和示例配置，不包含 `.runtime`、测试数据或生产密钥 | 通过 |
| 生产密钥外部托管 | 5 项生产密钥均为 macOS 登录钥匙串引用；逐项非空回读成功，生产配置权限 `600`、内联密钥 0，只读诊断和 `production:verify` 成功 | 通过 |
| 隐私擦除管理入口 | 本机管理台可按人员、项目或截止时间生成脱敏预览；接口强制双令牌、请求上限和单范围校验，响应只保留固定安全字段，页面不回显范围值；不存在管理台删除路由。浏览器实测桌面三列表单与 390px 单列布局，页面无横向溢出 | 通过代码、接口与浏览器验收；真正删除仍须具体范围授权和命令行执行 |
| 监听范围与群聊暂停 | 管理接口只返回联系人和群聊数量、16 位 HMAC 指纹、触发规则、发送能力和暂停状态，不含原始钉钉编号；双令牌只能暂停或恢复当前白名单命中对象。群聊暂停在草稿生成和已批准发送前均生效且不消耗重试，控制值在 PostgreSQL 加密保存并审计 | 通过代码、接口、Worker 与 PostgreSQL 集成验收；新增或删除监听目标仍须受控命令和服务重载 |
| 基于授权的能力自述 | 明确能力问题绕过 Codex，依据全局开关和请求人项目清单生成；禁用、过期和他人项目被排除，私聊不泄露路径/命令/人员编号，群聊不显示项目名称，清单异常时不猜测 | 通过识别边界、授权过滤、最小披露、安全降级和 Worker 不调用 Codex 测试 |
| Codex 只读控制入口 | 插件包含 7 个只读 MCP 工具和 MCP Apps 状态卡片；底层精确限制 5 个 GET 端点，只接受本机回环地址，从钥匙串读取只读令牌且不回显。当前主机已从本地仓库市场安装并启用 `0.2.0`，安装缓存与仓库一致；从缓存实际启动后 7 个工具全部只读，状态回读 `ready: true` 且真实发送关闭 | 当前主机通过代码、协议、安装、缓存一致性和真实只读链路验收；新环境仍需单独安装并现场复验 |
| 连续人工复核 | 判断质量页单条聚焦，一致标签直接保存并仅刷新质量数据，分歧标签强制填写原因，提交期间两个选择同时禁用；服务端继续按当前判断哈希拒绝过期标签 | 通过脚本解析、接口和全量回归；应用内浏览器安全策略拒绝内存预览页，本轮未执行视觉截图验收 |
| 可复用生产发布 | 当前主机完成 14 迁移兼容基线与当前版本两步发布、两次加密备份、015/016 迁移、原子切换和现场服务回退恢复；自动回退增加版本外应用服务清理 | 登录会话生产路径通过；GitHub Runner 因后台会话无法访问登录钥匙串已停用，不能视为可用发布通道 |
| 提交级云端门禁 | Actions 内部路径使用跨平台 ASCII，界面名称保持中文；`push` 已自动触发“检查”和“安全扫描”。`ci:verify` 先核对干净工作区与远端完整 SHA，再复用、等待或补触发缺失工作流，最终拒绝旧提交和失败结果 | 通过真实自动触发、完整 SHA 绑定和重复执行验收 |
| gbrain 知识边界 | 新项目默认关闭；开启时必须配置 slug 前缀白名单、页数和正文上限；只调用精确 `get_page`，禁止模糊、删除页和写入，后续 Codex 步骤需显式引用 | 通过代码验收，真实项目尚未授权 |
| 隔离复用安装 | 从真实 tarball 安装到全新临时目录；139 个发布文件通过门禁，55 个源码模块和 6 个 Codex 插件分发关键文件完成安装后校验；配置和项目清单均为 `600`，重复初始化被拒绝，外部能力默认关闭，全程不访问生产系统 | 通过 |
| 隔离工作树可靠性 | 新建工作树在补丁应用前强制对齐目标提交、刷新索引并检查干净状态，随后依次执行索引级补丁预检和应用；失败时事务化清理工作树、分支和空运行目录。修复前确认旧测试清理会遗留 89 个空目录；清理并修复后完成 16 轮、8 路并发压力运行，全部通过且无空目录残留 | 通过；历史索引不匹配未稳定复现，因此不把空目录残留表述为其唯一根因 |
| 记忆可携带与删除 | 管理接口展示来源；纠正显式替代；元数据导出默认无正文，完整导出需固定确认且文件为 `600`、不可覆盖；永久删除确认值绑定具体编号，正文和来源等字段被空密文覆盖，审计不含业务正文 | 通过代码验收；未对生产记忆执行删除 |
| gbrain 记忆来源权限 | 确认前实时复核，后台每 5 分钟按项目能力、授权期限、精确 slug、页面可读性和版本续签短期租约；租约到期立即停用，撤权在下一次复核传播，临时故障不永久删除 | 迁移 015 与第 9 个 LaunchAgent 已生产发布；当前项目未启用知识读取，因此生产诊断显示 gbrain 非必需 |
| 数据范围擦除 | 人、项目、绝对截止时间三选一；预览只返回脱敏指纹和数量；确认绑定完整快照；活动/未决数据整体阻塞；消息删除后以 HMAC 指纹防止回看复活，任务、计划、记忆、审批、证据、标注和相关审计去正文 | 迁移 016 已生产应用；未对生产数据执行任何删除 |
| 数据库结构兼容性 | 统一读取 16 个迁移文件并核对生产 `schema_migrations` 版本与 SHA-256；预检允许报告待迁移项，严格诊断、影子验收和 PostgreSQL 运行时在业务查询前拒绝缺失迁移；影子验收使用 PostgreSQL 会话级只读模式且不创建加密哨兵 | 通过单元、隔离数据库和真实生产只读复测；生产为 16/16，无待迁移、漂移或意外版本 |
| 迁移后的服务回退 | 迁移 015、016 已登记 `service_only` 兼容证据；14 迁移基线 `07383a2` 具备统一迁移预检格式。现场在 16 迁移数据库上恢复旧 8 服务并通过验证，再恢复当前 9 服务；自动回退会清理并备份新版本独有服务，且明确排除 PostgreSQL LaunchAgent | 代码、隔离数据库与真实生产服务恢复通过；数据库未做破坏性反向迁移 |

## 3. 尚未达到的放量条件

| 条件 | 当前状态 | 处理方式 |
|---|---|---|
| 100 条真实人工判断标注 | 2026-08-06 使用 PostgreSQL 会话级只读连接复核：`decision_reviews` 共 0 条，其中绑定判断版本 0 条，即 0/100；未读取消息正文 | 在管理台“判断质量”按风险与覆盖队列人工标注，禁止 AI 自己补标签 |
| 分层覆盖 | 尚未达到 | 同时覆盖回复/不回复、私聊/群聊、硬规则/Codex/人工复查来源 |
| 不回复准确率 ≥ 95% | 样本不足 | 满足样本与覆盖后再计算 |
| 高风险错误回复建议为 0 | 尚无充分样本 | 发现一条即阻止放量 |
| 滚动 30 天入口可用性 ≥ 99.5% | 观察窗口未满 | 系统持续分钟采样，满窗口前 `targetMet` 保持未知 |

## 4. 复用验收流程

```mermaid
flowchart LR
    A[克隆代码并安装依赖] --> B[生成受保护的生产配置]
    B --> C[创建项目能力清单]
    C --> D[只读生产诊断]
    D --> E[加密备份并迁移数据库]
    E --> F[安装 9 个常驻服务]
    F --> G[生产健康与真实消息对账]
    G --> H[影子人工标注]
    H --> I[满足门槛后单独审批放量]
```

新环境必须使用自己的租户、联系人、项目目录、数据库、密钥和审批人。示例配置只说明结构，不构成任何真实权限。生产发布工作流在迁移前强制执行加密备份，任何预检、备份、迁移或验证失败都会停止后续步骤。

## 5. 验收边界

- 本报告不把“进程启动”当作业务放量，也不把短期可用性观察值当作月度达标。
- OA 审批同意、拒绝、转交以及通用生产数据修改仍属于禁止自动执行能力。
- 自动发送和计划自动执行保持关闭；开启必须另有明确授权，并满足影子质量和真实项目清单门槛。
- Codex CLI 最近一次只读诊断已通过运行校验；版本检查存在非阻断告警。生产健康检查不代替真实草稿质量验收，放量前仍应重新运行真实草稿探针。
