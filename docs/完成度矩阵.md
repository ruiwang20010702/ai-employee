# AI 员工完成度矩阵

这张表只回答三件事：已经具备什么、证据在哪里、还差什么。代码通过不等于生产放量，生产运行、人工质量和长期观察分别判断。生产状态快照截至 2026 年 8 月 11 日，后续发布必须同步更新本表和验收报告。

统一口径：“已实现”表示代码和自动测试具备；“已部署”表示当前主机正在运行；“已授权”表示具体项目和请求人可以使用；“可放量”还必须同时通过健康、人工质量和长期指标门槛。四者不能互相替代。

| 目标 | 当前证据 | 当前判断 | 还差什么 |
|---|---|---|---|
| 可靠发现钉钉消息 | DWS 分页、活动唤醒、定时补漏、消息唯一键和独立对账均有单元及 PostgreSQL 集成测试 | 本地候选已实现；生产监听心跳和最近对账正常 | 持续观察真实漏检率 |
| 判断什么时候不回复 | 硬规则、Codex 上下文复核、AI 自发消息排除、人工已回复检查、私聊和群聊反向测试 | 已实现；当前有效人工判断 42/100 | 还需 58 条有效判断；群聊已复核 7/10，还需 3 条覆盖 |
| 评价回复草稿是否可用 | 回应标签绑定判断哈希；草稿评价绑定实际草稿哈希；草稿变化、无草稿、旧评价和过期提交均有测试 | 新版管理台已发布，生产页面标记与控制台回读通过 | 完成至少 30 条真实草稿评价 |
| 信息不足时追问 | 独立等待状态、送达确认时间、同发送人同会话唯一关联、模型复核、歧义拒绝、超时、失败恢复、人工接管和 SQLite/PostgreSQL 对等测试 | 已随第 017 号迁移部署生产；真实发送仍关闭，因此不会未经审批自动发出追问 | 在影子样本中持续验证关联准确率；业务放量仍受发送、质量和项目授权门禁控制 |
| 安全发送消息 | 单次审批、草稿哈希、人工回复复查、幂等副作用账本和平台明确成功回执均有正反向测试；失败或空回执进入结果未知 | 能力已实现，生产真实发送关闭 | 质量门槛通过后另行审批放量 |
| 完成方案、文档、代码和项目任务 | 项目清单、完整计划风险、能力网关、隔离工作树、固定测试命令和结果回传均有端到端测试 | 通用执行底座已实现，计划执行默认关闭 | 每个真实项目单独登记范围、命令、验收和回滚 |
| 真实项目授权 | 项目清单支持请求人、目录、能力、有效期、次数、超时、固定目标和回滚 | 当前生产授权项目为 0，因此通用执行适配器不会获得真实项目权限 | 逐个项目建立最小权限清单并完成只读检查和现场演练 |
| 控制高风险动作 | 完整计划按最高风险审批；推送、共享写入和生产发布均绑定目标、审批、回读与回滚 | 已实现，默认拒绝未登记能力 | 真实项目逐项授权；生产动作仍需单次审批 |
| 授权预算 | 第 018 号迁移建立项目、授权版本与能力三者绑定的持久次数账本；执行开始原子扣减，跨计划继续累计，失败或取消不返还 | 时间与次数门禁已部署生产；当前项目授权为 0，费用估算和实际费用核销尚未闭环 | 接入可信用量回执后再实现费用上限和超额停止 |
| 正式记忆 | AI 只形成绑定本批真实消息来源的 `proposed` 候选；凭据、连接串和敏感人物评价入库前拒绝；去重、冲突、人工确认、过期、撤销、导出和擦除均有 SQLite/PostgreSQL 测试 | 自动候选链已部署但绝不自动确认；当前生产正式记忆为 0 | 在真实工作中逐条人工确认，观察误提案与冲突率 |
| 人工暂停和接管 | 全局/局部暂停、任务取消、进程组中断、外部副作用安全收尾和租约异常均有测试 | 已实现 | 生产演练任务级接管，不触发真实副作用 |
| 运行健康和恢复 | 心跳、深度健康、全部健康阻塞原因、加密备份、隔离恢复、版本回退和迁移兼容门禁均有测试；服务部署验证逐一绑定 9 个 LaunchAgent 的目标版本 | V2.2 技术版本已部署；服务可用，但生产仍有 2 条既有死亡任务，因此业务 `ready: false` | 负责人逐条核对死亡任务；处理前继续保持业务门禁关闭 |
| 异常主动触达 | 告警支持脱敏本机日志、HTTPS 与 HMAC 签名，并覆盖死亡任务、失败计划、执行计划和过期租约 | 代码可用；当前未配置独立外部告警地址，无人查看本机日志时不会主动触达负责人 | 选择并授权钉钉或其他 HTTPS 告警接收端，配置后做真实到达回读 |
| 隐私和秘密保护 | 业务字段加密、钥匙串外部托管、子进程密钥隔离、脱敏错误、范围擦除和只读会话均有测试 | 已实现 | 新环境必须使用自己的密钥、租户和数据范围 |
| 可复用安装与升级 | 真实 tarball 安装到两个空目录，校验 11 个上线入口预览、全部迁移清单、路径隔离和零生命周期写入；本机固定提交发布器默认预览并复用云端门禁、备份、迁移、服务验证、兼容回退和第 018 号维护前滚 | 自动复用验收通过；同一发布器已完成当前主机真实维护前滚，配置权限 `600`、拒绝覆盖、外部能力默认关闭 | 物理第二台 Mac 仍需独立 DWS/Codex 授权、PostgreSQL 迁移和现场影子验收 |
| 生产放量 | V2.2 技术版本已部署；自动发送和计划执行保持关闭；统一只读验收组合质量、健康和长期 SLO，并对未知值失败关闭 | 技术发布完成，业务自动化尚未放量；当前消息发现和低风险草稿耗时 P95 均未达标 | 处理死亡任务、补齐 58 条回应判断、30 条草稿评价、3 条群聊覆盖及未知来源 1 条，优化真实处理时延并完成滚动 30 天可用性观察；达到全部门槛后仍需单独批准放量 |

## 统一上线顺序

```mermaid
flowchart LR
    A["填写独立生产配置"] --> B["只读迁移预检"]
    B --> C["加密备份"]
    C --> D["显式数据库迁移"]
    D --> E["严格只读诊断"]
    E --> F["合成 Codex 草稿探针"]
    F --> G["安装或更新服务"]
    G --> H["目标版本服务部署验证"]
    H --> I["严格业务就绪与影子验收"]
    I --> J["人工质量和长期 SLO 门槛"]
    J --> K["另行审批放量"]
```

任何一步失败都停止后续步骤。检查不能暗中迁移，迁移不能暗中部署，部署不能自动打开发送或计划执行。
