# Foursday 当前状态与删除区

更新时间：2026-08-19
规则：本页只记录当前状态；产品定义看 PRD，结构看技术设计，历史原因看迁移方案和 Changelog。

## 状态摘要

| 项目 | 当前状态 | 权威证据 |
|---|---|---|
| 产品定位 | V3 已切换为“个人记忆驱动的 Hermes 工作分身” | [产品需求文档](./产品需求文档.md) |
| Hermes 上游 | 固定 `v2026.8.18` / `0.20.4` / 完整提交 `e624e9f…` | `hermes/upstream.lock.json` |
| Fork 范围 | 仅三文件 Session workspace 薄补丁；有摘要锁和上游契约测试 | `hermes/patches.lock.json` |
| DWS 个人钉钉 | 真实接收、白名单、项目路由、本人发送/接管、娜娜原会话自然回复均已回读 | [迁移验收报告](./自主工作分身迁移验收报告.md) |
| 连续消息 | 3 秒安静窗口、8 秒总上限；较晚消息复用 Session 和持久项目绑定 | Python/Node DWS 测试 |
| 项目路由 | 最小注册表、最长别名、路径防误命中、歧义澄清、跨重启绑定 | `hermes/plugins/project_router/` |
| 个人 gbrain | `default + read-only OAuth` 精确项目页；密钥停留在宿主桥接 | `src/hermes-personal-memory-context.mjs` |
| 通用 Agent Loop | 2.2 查数、连续追问、真实文档/分析/代码修改通过 | [迁移验收报告](./自主工作分身迁移验收报告.md) |
| 工具隔离 | 每次终端调用进入无网络项目沙箱；未注册目录、`.runtime`、钥匙串和系统控制拒绝 | `hermes/plugins/foursday_boundary/` |
| 高风险出口 | push、merge、release、部署、生产 SQL、不可逆删除、付款、合同、人事、秘密和不可撤销承诺硬阻断 | 边界插件及反向测试 |
| 发行层 | 默认零写；三类插件、Profile、Skill 可恢复安装并拒绝覆盖内置工具 | `scripts/安装Hermes发行层.mjs` |
| 测试 | Foursday 901 通过、0 失败、67 条条件跳过；Hermes Python 34；依赖漏洞 0 | 当前回归与 Gate 2 证据 |
| 当前生产 | Hermes `active` 是唯一消息写入者，`send=true`、检查点健康；旧 listener/worker/executor/proactive 全部停止 | active 回执、LaunchAgent 与状态回读 |
| Shadow 恢复 | DWS 全量检查点健康；launchd `kickstart -k` 后 Gateway PID 更新、检查点继续推进且未重放消息 | `com.foursday.hermes-gateway` 与私有状态文件 |
| Active 切换门禁 | 十项验收已绑定精确提交；已完成 active → legacy → active 真实回退演练 | active/rollback/final active 三份私有回执 |
| 生产业务就绪 | Hermes active 运行中，DWS 检查点健康；9465 管理面继续可用 | active 状态与 HTTP 回读 |

## Gate 2 结论

12 项 PoC 和十项生产 shadow 门槛全部通过，Hermes 已完成单写者 active 切换与真实回退演练。以下操作仍需独立处理：

- 提交、推送、创建或合并 PR；
- 发布 Release；
- 发布新的稳定 Release 标签；
- 删除仍承担在线回退的旧 Runtime；
- 扩大自动发送或主动工作范围。

## 删除区

以下概念已从个人默认架构撤销。没有新的架构决策和验收证据，不得重新引入：

| 已撤销概念 | 为什么撤销 | 替代方案 | 允许重新评估的条件 |
|---|---|---|---|
| `project_evidence_read` | 每个业务问题都需要专用代码 | Hermes/Codex 在真实项目自主检索和计算 | 只可作为可选企业确定性工具，不得成为个人默认入口 |
| `produced_questions` 等固定指标 | 把 Agent 退化成指标问答机器人 | 当前文件证据 + 通用 Agent Loop | 外部系统提供正式 typed API 且作为工具而非回答模板 |
| 固定 JSON Pointer 回复 | 与业务文件结构强耦合，扩展成本高 | Agent 读取脚本、台账和汇总并引用证据 | 企业审计场景单独启用 |
| 逐联系人项目 requester | 重复人工配置，阻止可信联系人工作 | 入口 allowlist + 项目自动路由 | 多租户企业组织隔离模式 |
| 固定业务回复模板 | 不能处理追问、解释和新问题 | 自然生成 + 数字/文件回读 | 法规要求固定披露文本 |
| 自研完整 Agent Loop | 重复 Hermes/Codex 成熟底座 | 固定上游 + 外部插件/Profile/Skills | Hermes 无法通过扩展或小补丁支持核心需求 |
| 独立 Foursday 长期知识库 | 与个人 gbrain 重复、冲突和删除漂移 | 个人 gbrain 唯一长期权威；Hermes DB 只存运行上下文 | 多用户产品需要真正租户隔离时重新设计 |

## 兼容保留区

旧 `project manifest / capability / budget / approval / recipe / step adapter` 已降级为 Enterprise / Governed Mode。旧 Node Runtime 代码与 LaunchAgent 暂时只承担已验证回退；active 稳定窗口结束并具备 Hermes 原生发布/回退链后再删除，不能反向定义 V3 个人模式。

## 下一步

1. 观察 active 运行与真实消息回执；
2. 建立 Hermes 原生不可变版本发布和 active 更新链；
3. 稳定窗口结束后删除旧 Node Runtime、旧 LaunchAgent 和兼容测试；
4. 继续扩展飞书、Slack、Teams 等消息入口。
