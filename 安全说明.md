# 安全说明

## 报告问题

请通过 GitHub Security Advisory 私下报告安全问题，不要在公开 Issue 中提交：

- DWS 或钉钉凭证
- API 密钥和访问令牌
- 真实联系人 ID
- 私聊内容和运行日志
- 企业内部文档或代码

## 数据边界

- 仓库代码不会读取钉钉私有数据库正文。
- 本地文件变化只作为活动信号，消息事实通过 DWS 获取。
- `.runtime/` 可能包含消息正文和任务状态，必须保持在本机且不得提交。
- `.env` 和其他本地配置必须保持未跟踪状态。

## 当前安全状态

当前版本已经具备消息入库、独立草稿 Worker、人工审批、能力开关和幂等发送，但默认不开启真实发送：

- 只配置 `draft_reply` 时不会发送钉钉消息。
- 开启发送还需要 `send_message`、自身 userId 和逐任务审批。
- 发送前重新检查负责人是否已经人工回复。
- 发送结果未知时停止，不自动重发。
- Codex 草稿使用只读沙箱，聊天正文不能授予能力。
- 消息正文、任务载荷、草稿、审批原因和发送回执使用 AES-256-GCM 字段级加密。
- 未注入 `AI_EMPLOYEE_DATA_KEY` 时使用数据库旁的本机密钥文件；正式环境仍应接入独立密钥管理。
- 尚未完成多租户或跨用户隔离。
- 代码、共享文档、项目发布和生产部署能力尚未开放。
- 不应绕过审批或能力开关用于无人监督的高风险操作。
