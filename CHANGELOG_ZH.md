# 更新日志

[English](./CHANGELOG.md) · **简体中文**

本项目的重要变化记录在此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循语义化版本。

## [未发布]

本节记录 `v0.6.0-rc.1` 之后的候选改动；在后续精确提交发布前，它们
不属于当前带标签预览版或生产版本。

### 新增

- 新增生产形态的 Hermes shadow 服务：Application Support 隔离版本、Node 监督的 launchd Gateway、私有全量 DWS 检查点、全只读项目注册表、重启进程归属回读，以及失败关闭的单写者切换状态机；旧 Runtime 仍保持唯一发送权。
- 新增默认只预览的 active 切换命令：必须绑定精确发布 SHA、七天内十项 shadow 验收、旧数据库零在途、严格停旧顺序、active 回读，以及“先停 Hermes 再恢复旧服务”的失败回退。
- 新增隐私受限的 shadow 证据账本与验收生成器；消息、参与人、会话和回复身份只保存哈希，不保存正文，发现重复投递会阻断切换。
- 新增发布身份清单，把精确 Git SHA 与 Gateway、补丁、桥接、边界和注册表关键文件逐一绑定；文件漂移和符号链接替换会在停止旧写入服务前失败关闭。
- 按 Hermes 新架构重新建立文档治理：精简中英文首页，确立 V3 产品宪法、Hermes-first 技术权威、实时状态与删除区矩阵，并给旧生产兼容文档增加明确范围说明，避免把旧治理 Runtime 误认为个人默认模式。
- 锁定 Hermes Agent `v2026.8.18` / `0.20.4` 作为 Foursday 候选运行时，使用精确上游提交、可复现隔离安装和仅三文件的 Session workspace 薄补丁，不做重度 Fork。
- 新增 DWS 个人钉钉、最小项目路由、个人 gbrain 只读上下文和统一高风险工具边界四类外部插件。
- 新增通用 Hermes/Codex 影子运行器：保持项目与会话连续性，支持 macOS 只读沙箱，保存自然回复和工具证据，但不发送真实消息。
- 已在没有业务指标、JSON Pointer、固定回复模板和逐项目请求人配置的前提下，跑通 2.2 查数、连续追问、批次/成本/原因分析和自主代码修复。
- 新增有界连续消息合并、经核验的人工接管 interrupt、不含正文的撤回审计，以及缺少服务器消息 ID 时禁止自动重试的发送未知处理。
- 完成一次个人钉钉本人自聊真实发送：独立回读取得唯一服务器消息 ID，负责人真实回复触发 Hermes interrupt 与审计，对账期间没有发送第二条消息。
- 经独立收件人授权，通过个人钉钉账号向原会话发送 68,786 口径更正，独立回读精确正文只有一条且服务器消息 ID 与发送回执一致。
- 新增逐工具 macOS 工作区沙箱：模型认证留在宿主进程，终端拿不到生产/DWS/部署秘密和网络，也不能读取未注册项目。
- 新增默认零写、失败可恢复的 Hermes 发行层安装器，原子安装三类插件、Profile 和 Skill，并明确拒绝覆盖 Hermes 内置工具。
- 可选钉钉移动审批：待审消息幂等发送到本人自聊，手机短码批准或拒绝，并绑定有效期、一次性消费和事务内完整草稿哈希复核。
- 个人 PRIVATE gbrain Git 成为唯一长期可阅读记忆权威。Foursday 通过专用 OAuth client 读取个人 `default`；身份必须只有 `read`，不能带 `write/admin`。普通回复做有界检索，计划内知识读取仍要求精确 slug，不再复制第二套个人正文。
- Foursday PostgreSQL 只保存运行状态、治理证据、项目路由别名和待晋升候选。旧 overlay 写入器、删除回收与来源租约只保留迁移兼容，个人记忆模式不再运行。
- 新安装只创建受保护的 Foursday 配置，不再创建第二个 Markdown 仓库、gbrain source、`GBRAIN_HOME` 或 gbrain 数据库；个人 OAuth 凭据从外部密钥注入，预检实时回验身份。
- 无人值守回复新增独立的群聊和私聊追问开关。群聊仅限白名单中明确 `@我` 的低风险普通回复，私聊追问仅限一个必要问题；群聊追问、工作请求和中高风险继续审批。
- 项目知识读取固定绑定个人 `default` 中项目授权的精确 slug；固定来源项目记忆提取并入每 5 分钟记忆服务并只创建 PostgreSQL 候选。越权、无项目或能力未开放的执行请求返回确定性能力不足说明，不再依赖模型自行把握权限口径。
- 新增默认关闭的无人值守发送策略：只允许自动批准高置信、低风险、无需追问且不包含工作请求的私聊普通回复。群聊、计划、中高风险、人工接管复查、幂等和回执核对保持不变。
- 中英文 GitHub 首页已精简为导航入口，不再复制产品、体验、增长和运维手册；详细规则继续由既有专题文档分别负责。
- 私聊草稿现在会重建截至当前源消息的 24 小时有界会话，并用精确源消息识别“我/对方”；已确认项目身份别名只路由匹配项目的正式记忆，并让同一会话后续省略项目名时继续关联。两分钟内的续发属于同一审阅片段，后续未发送草稿会让旧草稿和旧审批失效，倒序完成也只保留最新任务。历史不可核对时安全重试，不再静默退化成单条回复。
- 新增一次性本机所有者注册页，填写登录名、可选邮箱别名并两次确认密码；现有读写令牌只完成一次所有权验证，成功后原子保存校验值、立即登录并永久关闭注册入口。
- 运维管理台和个人驾驶舱现在共享同一个本机用户名或邮箱加密码会话；原有只读/写入令牌继续作为 API 与恢复兼容通道保留。
- 新增默认只预览的 `config:set-admin-login` 命令，以带盐 `scrypt` 校验值创建所有者登录；命令行不接收密码，也不保存明文。
- 新增默认零写的“已确认影子证据纳入”流程；写入前重新核对证据 SHA、隔离账本、项目授权、配方基线与本地本人确认，并按证据摘要幂等。
- 第 022 号迁移把影子时间返还与生产工作计划分开保存，但个人驾驶舱会把两种确认证据统一计入北极星指标并显示来源数量。
- 只读配方步骤通过 `evidenceStepIds` 显式声明产物数据流；下游文档直接消费指定的前序研究证据，不再从步骤顺序猜测上下文。
- 每日总结配方新增按日期确定性读取仓库活动的首个节点，使报告能够引用授权路径范围内的精确提交与变更文件，不再从 README 状态推断当天工作。
- 日报还会消费按项目和日期绑定的终态受治理计划与步骤回读摘要；不会把历史证据原文重新注入模型，影子运行也只读隔离账本。
- 个人工作台中的配方必须先只读预览完整计划，再提交精确的已审计划哈希进行登记；登记仍不等于批准或执行。
- 个人工作台新增历史项目 JSON 导入预览，展示来源绑定候选、安全跳过、重复与冲突；只有手工输入当前 `IMPORT-...` 摘要才应用，且最多创建待审记忆候选，不触碰外部系统。
- 项目记忆同步可在个人工作台审阅：显式写令牌动作才调用已配置 Agent，只处理隔离的授权文件；生成包在服务端短期绑定十分钟，浏览器不能篡改，并且只能应用一次。
- 个人工作台现在可以直接设置项目记忆授权，不再要求手改 JSON。双令牌零写预览把当前清单、非符号链接普通来源文件、事实前缀、项目范围内保留期、最长 365 天的授权期限和自动确认策略绑定到精确 `MEMORY-AUTH-...` 确认值；应用只原子更新权限为 `0600` 的项目清单，不会开启全局能力。
- 待审项目记忆现在直接显示在所属项目卡片中。工作台最多返回 20 条当前项目候选，不跨项目；确认和拒绝继续要求写入令牌，冲突会展示现有正式事实并要求明确替代，已有重复事实不能再次确认。
- 个人工作台新增统一工作委托单，替代项目、会议、日报、记忆和 GitHub 配方逐项弹窗输入。所有类型化字段一次展示，先生成受控计划预览再登记；定时工作还会共同审阅首次时间、间隔、每日次数与冷却，保存后仍停用，已审计划哈希变化时失败关闭。

### 安全

- 陌生联系人和未 `@` 的群消息不会创建 Hermes Session；项目歧义时只追问，不猜测。推送、合并、发布、生产数据库写入、不可恢复删除、付款、合同、人事和秘密访问独立于模型提示词硬阻断。
- 边界配置异常时失败关闭；Web 工具拒绝携带疑似凭据的出站文本，DWS 在桥接前拒绝秘密材料和不可撤销承诺。
- 注册只允许同源本机页面，必须同时提供既有读写令牌，拒绝配置符号链接和已存在账户，也不在浏览器持久化密码或首次验证令牌。密码会话只保存在内存中，时长限制为 5 分钟至 24 小时；连续失败会限流，Cookie 使用 `HttpOnly; SameSite=Strict` 并仅服务本机回环页面，所有非 GET 请求还必须携带独立 CSRF 令牌。登录不会取消计划、摘要等内容级确认。
- 影子证据纳入不会伪造生产工作计划，也不会导入项目记忆；应用必须使用证据摘要派生的确认口令，并继续受项目隐私擦除覆盖。
- 影子研究只使用历史导入时已经核验的项目路径；下游证据会校验类型、限制总大小，并始终按不可信业务数据处理。
- 项目记忆授权过期后，手动生成预览和自动同步循环都会在调用模型前停止。

### 计划中

- 更易安装的桌面发行包和生产级社区连接器。

## [0.6.0-rc.1] - 2026-08-13

### 新增

- 新增默认零写的项目配方影子预览、显式本地运行入口和证据 SHA 绑定的本人审阅确认；把已选择的研究/文档配方限制在同一干净 Git 快照与隔离证据账本内。
- 在个人工作台和 Codex 只读工具中新增按确认证据排序的“本周工作返还队列”，显示八小时目标缺口，但不会创建或执行计划。
- 已完成配方形成时间返还提案前，个人工作台会展示有界的研究与文档交付正文；本人投入统一指 AI 交付后的真实阅读、核对、纠正与修改时间。
- 历史项目导入、固定来源项目记忆同步、5 个版本化配方，以及 SQLite/PostgreSQL 受治理工作图。

### 安全

- 项目配方影子运行拒绝代码、记忆、办公、消息、Git 和发布能力；模型运行后重新核对来源摘要和提交；本地审阅确认保持幂等，且绝不创建生产记忆或时间返还记录。
- 周计划只使用本人确认的结果，未知能力模式或缺少周计划接口的旧服务均失败关闭，排除活动中的重复配方；Codex 只访问专用有界接口，项目目标、记忆正文、图数据、计划载荷或被禁能力明细不会进入插件进程。

## [0.5.0-rc.1] - 2026-08-13

### 新增

- 新增 10 分钟本机 Web 接入路径；预览无需钉钉、DWS 或生产 PostgreSQL。
- Codex、Claude Code 和 OpenAI-compatible 运行时共享同一条从 GitHub Issue 到已回读 Draft PR 的审批闭环。
- 新增隐私受限的闭环证据包，以及 10 次维护者自测和 10 名外部体验者的证据验证器。
- 英文默认 README、Foursday 新视觉、75 秒真实演示契约和 5 个边界清晰的首次贡献草稿。
- 个人项目接入、4 个官方配方、项目驾驶舱和本人确认的时间返还账本。
- 默认停用的定时/事件触发器，以及幂等运行、每日次数、冷却和租约。
- 会议到执行与 GitHub 补丁到 Draft PR 的完整配方。
- 带版本的工作事件/办公空间适配器契约，以及 Slack、Teams、Gmail、Google Workspace 示例。

### 安全

- 配方形成的项目记忆始终保持候选，必须本人确认，并随请求人或项目来源范围受控擦除。
- 主动工作复用消息任务的项目授权、审批、预算、人工接管、幂等和回读门禁。
- 项目接入在共享服务边界校验规范 Git 仓库根目录，不再只依赖 CLI 检查。
- 触发器定义拒绝凭据材料，定时运行绑定租约实例，主动计划必须等待精确运行完成落账后才能消费授权。
- 社区扩展清单只接受 `1.0` 契约版本，并要求五项安全保证全部显式开启。

## [0.4.0] - 2026-08-11

### 变更

- 项目正式更名为 **Foursday**，定位为开源工作分身，长期使命是每周为每位用户返还一个经过验证的工作日。
- `foursday` 成为主 CLI；`0.x` 版本继续保留 `ai-employee` 兼容别名。
- Codex 市场、插件、MCP 服务、技能和状态资源统一迁移为 Foursday，并提供明确迁移步骤。

### 新增

- 版本化渠道、智能体运行时和模型提供方契约。
- 无需企业账号或模型凭据的五分钟本地演示。
- 在钉钉 DWS 与 Codex 之外，新增飞书开放平台与 Claude Code 集成契约。

## [0.3.0] - 2026-08-11

### 新增

- 项目能力清单、工作计划、审批哈希、租约和持久次数预算。
- 来源绑定的正式记忆候选、人工确认、冲突替代、有效期、撤销和隐私擦除。
- 信息不足等待链、连续消息有界合并和跨状态人工接管。
- 钉钉办公动作、代码工作和生产发布适配器的目标回读验证。
- PostgreSQL 生产存储、字段级加密、备份恢复、健康告警和 SLO 验收。
- 精确提交门禁、不可变版本目录、崩溃恢复日志和前滚迁移边界。

### 安全

- 子进程使用最小环境，避免把生产密钥传给 Codex、DWS 或候选代码。
- 外部副作用在执行前登记；结果未知时禁止自动重试。
- 能力预算由数据库持久约束保护，旧服务无法绕过第 018 号迁移。

### 文档

- 重构中英文项目首页，补充架构、能力边界、快速开始与路线图。
- 补齐贡献指南、安全策略、行为准则、Issue 和 PR 模板。

## [0.2.0] - 2026-08-05

### 新增

- DWS 消息监听、草稿生成、人工审批和发送回执。
- 本机管理台、只读 Codex 插件和 macOS 常驻服务。
- SQLite 开发存储与基础生产诊断流程。

[未发布]: https://github.com/ruiwang20010702/foursday/compare/v0.6.0-rc.1...HEAD
[0.6.0-rc.1]: https://github.com/ruiwang20010702/foursday/releases/tag/v0.6.0-rc.1
[0.5.0-rc.1]: https://github.com/ruiwang20010702/foursday/releases/tag/v0.5.0-rc.1
[0.4.0]: https://github.com/ruiwang20010702/foursday/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/ruiwang20010702/foursday/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/ruiwang20010702/foursday/releases/tag/v0.2.0
